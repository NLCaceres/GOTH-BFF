package htmx

templ Link(name string, url string, target string) {
  <a hx-post={"/" + url} hx-target={target} hx-push-url="true"
     hx-trigger={"click[location.pathname !== '/" + url + "']"}>
       { name }
  </a>
}
